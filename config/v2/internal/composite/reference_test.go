package composite

import (
	"testing"
)

func TestReference(t *testing.T) {
	// g := &Datasources{}
	// r := Reference[Datasource, *Datasources]{
	// 	global: g,
	// }
	//
	// build, err := r.Build()
	// require.NoError(t, err)
	// // build["name"].
}
